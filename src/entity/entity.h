#ifndef ENTITY_ENTITY_H
#define ENTITY_ENTITY_H

/* ECS-lite type tags. Individual entity definitions in their own headers. */
typedef enum {
    ENTITY_BLOB,
    ENTITY_ENEMY,
    ENTITY_PROP,
} entity_kind_t;

#endif
